var constants = require('../lib/constants');

function start(loginData, scrapModules, callback) {

    if(loginData.email && loginData.password) { // true if both variables are not empty
        scrapModules();
        callback();
    }
    else {
        throw constants.MSGS_INVALID_LOGINDATA;
    }
}

exports.start = start;